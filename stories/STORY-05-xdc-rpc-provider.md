# Story 05 — RPC Provider

## Goal

Create reusable XDC provider.

## Tasks

Create:

lib/xdc/provider.ts

Expose:

getProvider  
getBlockNumber  
getBalance  

## Acceptance Criteria

App can fetch block number.